apiVersion: v1
kind: Service
metadata:
  name: randomcars-api
  labels:
    app: randomcars
spec:
  type: ClusterIP
  selector:
    app: randomcars
  ports:
    - name: http
      port: 80
      targetPort: 8000